"use client";
import React from "react";

const SelectCityCard = () => {
  return (
    <button onClick={() => console.log("cidade selecionada")}>
      Mudar Carta
    </button>
  );
};

export default SelectCityCard;
